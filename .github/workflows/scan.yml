name: WordPress WPScan

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  wpscan:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build and start WordPress
        run: |
          docker compose -f docker/docker-compose.yml up -d --build

      - name: Wait for WordPress
        run: |
          echo "Waiting for WordPress to start..."
          for i in {1..30}; do
            if curl -s http://localhost:8080 | grep -q "<title>"; then
              echo "WordPress is ready."
              exit 0
            fi
            sleep 5
          done
          echo "WordPress did not start in time."
          exit 1

      - name: Wait for plugin install
        run: |
          echo "Waiting for wp-cli to install plugins..."
          for i in {1..30}; do
            if docker logs docker-wp-cli-1 | grep -q "WP-CLI plugins installed"; then
              echo "Plugins installed."
              exit 0
            fi
            sleep 5
          done
          echo "Plugins were not installed in time."
          docker logs docker-wp-cli-1 || true
          exit 1

      - name: Run WPScan
        env:
          WPSCAN_API_TOKEN: ${{ secrets.WPSCAN_API_TOKEN }}
        run: |
          mkdir -p scans
          TS=$(date +"%Y%m%d-%H%M%S")
          docker run --rm --network host wpscanteam/wpscan \
            --url http://localhost:8080 \
            --enumerate u,vp,vt,tt \
            --plugins-detection aggressive \
            --api-token "$WPSCAN_API_TOKEN" \
            --format json --output "scans/wpscan-${TS}.json" \
            --no-color --no-update --force

      - name: Upload scan artifact
        uses: actions/upload-artifact@v4
        with:
          name: wpscan-results
          path: scans/
